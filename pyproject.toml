[project]
name = "studio-mc-lesatelier"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "python-pptx",
    "python-docx"
]

[tool.vercel]
entrypoint = "api.generate:handler"
